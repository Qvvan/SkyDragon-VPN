from handlers.services.create_keys import encode_numbers


async def create_config_link(user_id: int, sub_id: int):
    sub_uuid = encode_numbers(user_id, sub_id)
    url = "https://skydragonvpn.ru/sub/"

    return url + sub_uuid
