# Приоритет: реальные переменные окружения > этот файл > config.yaml

# Обязательные секреты
DSN=postgresql+asyncpg://USER:PASSWORD@HOST:5432/DBNAME
CRYPTO_KEY=

# Сгенерировать ключ Fernet:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# YooKassa (продление через сайт)
SHOP_ID=
SHOP_API_TOKEN=
